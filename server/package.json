{
  "name": "sih-26047-backend",
  "version": "1.0.0",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "build:client": "cd ../client && npm install && npm run build && mkdir -p ../server/public && cp -R dist/. ../server/public/",
    "start": "node server.js",
    "dev": "node --watch server.js",
    "postinstall": "npm run build:client"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "multer": "^1.4.5-lts.1"
  }
}